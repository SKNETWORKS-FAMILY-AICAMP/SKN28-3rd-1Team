# Frontend Varlock schema.
#
# @defaultSensitive=false @defaultRequired=false
# @currentEnv=$APP_ENV
# ---

# @public @type=enum(local,dev,staging,prod)
APP_ENV=dev

# Server-side backend base URL used by the App Router chat BFF.
# @type=url
BACKEND_URL="http://127.0.0.1:8000"
