# `src/ui/primitives`

Low-level UI primitives.

Use this for shadcn-generated primitives and thin framework-level wrappers such as buttons, inputs, dialogs, scroll areas, and menus. Keep these components generic and free of product or route semantics.

Components in `src/ui/components` may compose these primitives.
