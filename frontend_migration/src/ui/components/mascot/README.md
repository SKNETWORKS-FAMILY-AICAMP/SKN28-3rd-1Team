# `src/ui/components/mascot`

Product-wide 로디 캐릭터 components.

Use this directory for mascot rendering primitives that can appear in chat, home, mocks, headers, and future agent-rendered surfaces. Keep route-specific state orchestration in `src/page`, and keep chat-only layout or message components in `src/ui/components/chat`.
