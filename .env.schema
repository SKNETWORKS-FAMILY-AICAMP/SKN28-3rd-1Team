# Shared Varlock schema for the current project scope.
#
# This file is safe to commit. Real secret values must live in a local
# gitignored file or an external secret provider.
#
# @defaultSensitive=false @defaultRequired=false
# @currentEnv=$APP_ENV
# ---

# @public @type=enum(local,dev,demo,prod)
APP_ENV=local
